# Optional secrets — prefer env vars over config.yaml on VPS
# Copy to .env and: export $(grep -v '^#' .env | xargs)

RECLAIMKIT_SMTP_PASSWORD=
RECLAIMKIT_SLACK_WEBHOOK=
RECLAIMKIT_SERPAPI_KEY=
RECLAIMKIT_TINEYE_KEY=
